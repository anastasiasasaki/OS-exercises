# Set up the workplace 

Goal: Set up the workplace (Debian) so I can do all the projects.  

Issue1: git was not installed. trying to install gave error that I do not have access. use sudo install. SOLVED.  

Issue2: need to set up git SSH key because cloning gave error that I do not have access. 
Set up SSH key and cloned successfully. SOLVED.  

Try to add & commit & push. SUCCESS. 

DONE. 
